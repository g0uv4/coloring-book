# Image backends by host

This file is a pointer. The live router is [image-backend.md](image-backend.md).

Do not add a second table here. Detect the host once, call that session's image-edit tool, attach `plate-clean.png` in chat after QC, never invent HTTP APIs or ask for keys.
